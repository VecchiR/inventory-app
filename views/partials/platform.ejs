<li>
    <span>Platform: <span><%= platform.name%></span></span>
    <a href=<%= `/platform/${platform.id}/update` %>>Edit</a>
    <form action=<%= `/platform/${platform.id}/delete` %> method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this platform?');">Delete</button>
      </form>
</li>