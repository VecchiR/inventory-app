<li>
    <span>Tag: <span><%= tag.name%></span></span>
    <a href=<%= `/tag/${tag.id}/update` %>>Edit</a>
    <form action=<%= `/tag/${tag.id}/delete` %> method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this tag?');">Delete</button>
      </form>
</li>